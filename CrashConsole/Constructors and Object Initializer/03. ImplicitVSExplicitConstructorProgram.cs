public class ImpVsExp()
{
    // Implicit Constructor: When we do not create any constructor in class then compiler automatically initialises constructor in backend. This is called implicit constructor or default ctor.

    // Explicit Constructor: Any constructor we create for any of our operation is called as explicit constructor. All the constructor examples we did previously comes in explicit constructor.
}