using Encapsulation2;
public class Program1
{
    public static void Encapsulation2Main()
    {
        Encapsulation2i p1 = new Encapsulation2i();

        Console.WriteLine(p1.getProductID());
    }
}